---
description: "Generative Expand is a **canvas extension tool**, NOT a scene generator. It fills the expanded area by continuing the edge pixels of the existing image. The prompt field exists but has minimal influen → Replace Generative Expand with **Product Background** tool in the pipeline:

1. Product Background is designed specifically for scene generation
2. It accepts and respects scene prompts
3. It handles "
tags: [learning, learning, learning-event, blocker, unknown]
created: 2026-02-19
severity: blocker
video_id: vtest-qa
affected_tools: []
fix: Replace Generative Expand with **Product Background** tool in the pipeline:

1. Product Background is designed specifically for scene generation
2. It accepts and respects scene prompts
3. It handles 
---

# [BLOCKER] unknown: Identical Images from Generative Expand

## Symptom

Identical Images from Generative Expand

## Root Cause

Generative Expand is a **canvas extension tool**, NOT a scene generator. It fills the expanded area by continuing the edge pixels of the existing image. The prompt field exists but has minimal influen

## Fix Applied

Replace Generative Expand with **Product Background** tool in the pipeline:

1. Product Background is designed specifically for scene generation
2. It accepts and respects scene prompts
3. It handles
